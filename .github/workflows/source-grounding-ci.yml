name: Source Grounding CI

on:
  pull_request:
    paths:
      - app.py
      - lawglance_main.py
      - source_contract.py
      - requirements.txt
      - tests/**
      - Readme.md
      - .github/workflows/source-grounding-ci.yml
  push:
    branches: [main]
    paths:
      - app.py
      - lawglance_main.py
      - source_contract.py
      - requirements.txt
      - tests/**
      - Readme.md
      - .github/workflows/source-grounding-ci.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -r requirements.txt
      - run: python -m py_compile app.py lawglance_main.py source_contract.py tests/test_source_contract.py
      - run: python -c "import lawglance_main, source_contract; print('import smoke pass')"
      - run: python -m unittest tests.test_source_contract
